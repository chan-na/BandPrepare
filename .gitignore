# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# PyInstaller build artifacts (the bundles are huge; rebuild per platform).
# bandprepare.spec is a tracked source file, so *.spec is intentionally NOT ignored.
build/
dist/

# BandPrepare outputs
output/

# OS
.DS_Store
